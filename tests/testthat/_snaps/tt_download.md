# tt_download errors for bad file

    Code
      tt_download(tt, "bad_filename")
    Condition <tidytuesdayR-error-bad_file>
      Error in `tt_download()`:
      ! `files` must be one or more of "agencies.csv" or "launches.csv", or "All".

